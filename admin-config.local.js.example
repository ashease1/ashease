// Copy to admin-config.local.js and set your private password.
// admin-config.local.js is gitignored — never commit the real file.
window.__SS_ADMIN_CONFIG = {
  password: 'your-private-password-here'
};
